define(['backbone', 'models/class'], function(Backbone, ClassModel) {

    var ClassCollection = Backbone.Collection.extend({
        model: ClassModel,
        //url: config.base_url + 'assets/data/events.json',
        //url: Request.courses,

        initialize: function(models) {},

        parse: function(data) {
            return data.events || [];
        }
    });

    return ClassCollection;

});